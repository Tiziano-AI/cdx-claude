# cdx-claude Plan

## Remaining work

- Add import/apply helpers only after the inspect-first patch contract proves useful in real Claude work.
- Add Linux and Windows sandbox proof before expanding public `patch_autonomous` support beyond macOS.
- Add configurable sandbox network/domain policy after real autonomous shell workflows prove which hosts are genuinely needed beyond Claude backend access.
- Release the multi-directory read-scope contract only after explicit release authorization, version alignment, npm preflight, publish, marketplace upgrade, active MCP row verification, installed launcher help, installed MCP schema proof, installed doctor, and installed sandbox canary with `CDX_CLAUDE_NPM_SPEC` unset.
